@model IEnumerable<CRUD1.Models.Evento>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.QuantidadeEventosCalendario)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.QuantidadeEventosCalendario)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.NomeDoEvento }) |
            @Html.ActionLink("Details", "Details", new { id=item.NomeDoEvento }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.NomeDoEvento })
        </td>
    </tr>
}

</table>
