@model IEnumerable<CRUD1.Models.Calendar>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Quantidade)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.QuantidadeEventos)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Quantidade)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.QuantidadeEventos)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DayMounthYear }) |
            @Html.ActionLink("Details", "Details", new { id=item.DayMounthYear }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DayMounthYear })
        </td>
    </tr>
}

</table>
